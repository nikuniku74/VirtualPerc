diff --git a/modules/juce_audio_devices/native/juce_Audio_ios.cpp b/modules/juce_audio_devices/native/juce_Audio_ios.cpp
index c9857b2d78..c0e317c7ce 100644
--- a/modules/juce_audio_devices/native/juce_Audio_ios.cpp
+++ b/modules/juce_audio_devices/native/juce_Audio_ios.cpp
@@ -40,7 +40,7 @@ class iOSAudioIODevice;
 constexpr const char* const iOSAudioDeviceName = "iOS Audio";
 
 #ifndef JUCE_IOS_AUDIO_EXPLICIT_SAMPLERATES
- #define JUCE_IOS_AUDIO_EXPLICIT_SAMPLERATES
+ #define JUCE_IOS_AUDIO_EXPLICIT_SAMPLERATES 44100.0, 48000.0
 #endif
 
 constexpr std::initializer_list<double> iOSExplicitSampleRates { JUCE_IOS_AUDIO_EXPLICIT_SAMPLERATES };
@@ -220,10 +220,7 @@ namespace juce {
 static void logNSError (NSError* e)
 {
     if (e != nil)
-    {
-        JUCE_IOS_AUDIO_LOG ("iOS Audio error: " << [e.localizedDescription UTF8String]);
-        jassertfalse;
-    }
+        JUCE_IOS_AUDIO_LOG ("iOS Audio error: " << nsStringToJuce ([e localizedDescription]));
 }
 
 #define JUCE_NSERROR_CHECK(X)     { NSError* error = nil; X; logNSError (error); }
@@ -371,16 +368,11 @@ struct iOSAudioIODevice::Pimpl final : public AsyncUpdater
 
         if (category == AVAudioSessionCategoryPlayAndRecord)
         {
-           #if JUCE_IOS_API_VERSION_CAN_BE_BUILT (26, 0)
-            constexpr auto bluetoothOption = AVAudioSessionCategoryOptionAllowBluetoothHFP;
-           #else
-            constexpr auto bluetoothOption = AVAudioSessionCategoryOptionAllowBluetooth;
-           #endif
-
+            // No HFP (AllowBluetooth): that locks the session to 8–16 kHz and
+            // makes all mixed audio sound slow and distorted.
             options |= AVAudioSessionCategoryOptionDefaultToSpeaker
                      | AVAudioSessionCategoryOptionAllowAirPlay
-                     | AVAudioSessionCategoryOptionAllowBluetoothA2DP
-                     | bluetoothOption;
+                     | AVAudioSessionCategoryOptionAllowBluetoothA2DP;
         }
 
         JUCE_NSERROR_CHECK ([[AVAudioSession sharedInstance] setCategory: category
@@ -399,7 +391,11 @@ struct iOSAudioIODevice::Pimpl final : public AsyncUpdater
             {
                 SubstituteAudioUnit au;
                 [[maybe_unused]] const auto success = au.waitForAudioCallback();
-                jassert (success);
+                // Timeout is not fatal. On iOS 18+ this probe can miss the first
+                // callback (especially if the session was already touched), and
+                // jassert (success) stops Xcode with SIGTRAP on the message thread.
+                if (! success)
+                    JUCE_IOS_AUDIO_LOG ("SubstituteAudioUnit waitForAudioCallback timed out");
             }
         }
     }
@@ -508,10 +504,7 @@ struct iOSAudioIODevice::Pimpl final : public AsyncUpdater
                                               &audioQueue);
 
         if (err != noErr || audioQueue == nullptr)
-        {
-            jassertfalse;
             return {};
-        }
 
         const ScopeGuard disposeAudioQueueOnReturn { [&]
         {
@@ -527,10 +520,7 @@ struct iOSAudioIODevice::Pimpl final : public AsyncUpdater
                                                     &size);
 
         if (propErr != noErr || size != sizeof (result))
-        {
-            jassertfalse;
             return {};
-        }
 
         return result;
     }
